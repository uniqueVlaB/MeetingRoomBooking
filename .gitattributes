# Normalise line endings so the repository is identical for reviewers on Windows, macOS and Linux.
* text=auto eol=lf

# Windows-only tooling that expects CRLF.
*.sln  text eol=crlf
*.cmd  text eol=crlf
*.bat  text eol=crlf

# Binary assets.
*.png binary
*.jpg binary
*.ico binary
